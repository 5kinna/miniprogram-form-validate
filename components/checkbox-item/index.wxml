<view class="checkbox my-checkbox-item" catchtap="checkboxChange">
  <checkbox value="{{value}}" checked="{{checked}}" disabled="{{disabled}}" />
  <view class="checkbox-label">{{label}}</view>
</view>